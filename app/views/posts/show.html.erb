<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<%= action_links %>

<h3>Replys:</h3>
<ul>
	<% @post.replies.each do |reply|%>
	<li><%= reply.body %></li>
	<% end -%>
</ul>
	
	<%= form_for Reply.new, url: reply_post_path do |f|%>

	<%= f.text_area :body %>
	<%= f.submit %>

	<% end -%>	
