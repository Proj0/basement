<!-- 
<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th>Clicks</th>
      <th>Replies count</th>
      <th colspan="3"></th>
    </tr>
  </thead>
 -->
  <tbody>
    <% @pages.each do |post| %>
      <br>
        <td><h1><%= link_to post.title, post %></h1></td>
      <br>
        <td><%= post.body.truncate(30) %></td>
      <br>
<!--    
        <td><%= post.Clicks %></td>
        <td><%= post.replies_count %></td> 
-->
        <td><%= link_to '编辑', edit_post_path(post) %></td>
        <td><%= link_to '删除', post, method: :delete, data: { confirm: '你确定要删除当前文章内容吗？' } %></td>
      <br>
    <% end %>
  </tbody>
</table>
<div>
<%= link_to '发表文章', new_post_path, class: 'btn btn-default'%>
</div>

<center><%= paginate @pages %></center>